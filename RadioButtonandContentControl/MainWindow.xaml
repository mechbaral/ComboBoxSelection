<Window x:Class="RadioButtonandContentControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RadioButtonandContentControl"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <StackPanel>
        <StackPanel Orientation="Horizontal" Height="50" Margin="10,0,0,0">
            <RadioButton x:Name="Home" Content="Home" FontSize="20"  Style="{StaticResource {x:Type ToggleButton}}" Background="Transparent" Padding="5" Margin="1,1,1,1" BorderBrush="WhiteSmoke" IsChecked="True" Click="HomeClicked"></RadioButton>
            <RadioButton x:Name="CyinderOne" Content="Cylinder One" FontSize="20" Style="{StaticResource {x:Type ToggleButton}}" Background="Transparent" Padding="5" Margin="1,1,1,1"  BorderBrush="WhiteSmoke" Click="CyinderOneClicked"></RadioButton>
        </StackPanel>
        <ContentControl Content="{Binding}"/>
    </StackPanel>
</Window>
