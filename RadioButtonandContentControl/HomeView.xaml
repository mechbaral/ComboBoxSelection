<UserControl x:Class="RadioButtonandContentControl.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RadioButtonandContentControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Label Height="50" Content="HomeView" Background="Teal" FontSize="30" Foreground="White" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        <StackPanel Orientation="Horizontal" Margin="0,50,0,0">
            <Label FontSize="30" Content="Select the Items in the combobox" />
            <ComboBox x:Name="MyComboBox" ItemsSource="{Binding ListofItems}"   Width="150" Margin="10,0,0,0"/>
        </StackPanel>
        <Label Content="{Binding SelectedItem}" Width="500" Margin="0,59" Background="AntiqueWhite" Height="50" FontSize="30" />
    </StackPanel>
</UserControl>
